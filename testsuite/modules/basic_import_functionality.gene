#!/usr/bin/env gene run

# Test actual module import functionality
# Create an in-memory module using namespace
(ns math
  (fn square [x] (* x x))
  (fn cube [x] (* x x x)))

# Access module members
(print "Square of 5: " (math/square 5))
(print "Cube of 3: " (math/cube 3))