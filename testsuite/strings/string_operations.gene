#!/usr/bin/env gene run

# Test: String operations
(var hello "Hello")
(var world "World")

# String length
(print "Length of 'Hello': " (hello .length))

# String concatenation
(var space " ")
(var greeting (hello .++ space))
(print "Concatenation: " (greeting .++ world))